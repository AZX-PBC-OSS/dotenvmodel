# dotenvmodel

# :simple-python: Type-safe environment configuration with automatic `.env` file loading

**dotenvmodel** combines Pydantic-style field definitions with intelligent `.env` file
cascading inspired by Node.js dotenv patterns. Define your config once, get full type
safety, validation, and automatic `.env` loading — with only one runtime dependency.

---

## Features

<div class="grid cards" markdown>

-   :material-package-down:{ .lg .middle } **Minimal Dependencies**

    ---

    Only requires `python-dotenv`. No heavy frameworks, no transitive dependency trees.

-   :material-shield-check:{ .lg .middle } **Type Safety**

    ---

    Full type hint support with automatic type coercion. Your IDE and type checkers
    (mypy, pyright) understand every field.

-   :material-shape-plus:{ .lg .middle } **Rich Type Support**

    ---

    `UUID`, `Decimal`, `datetime`, `timedelta`, `SecretStr`, `HttpUrl`, `PostgresDsn`,
    `RedisDsn`, `Json[T]`, `Path`, and all standard Python collection types.

-   :material-file-cog:{ .lg .middle } **Smart `.env` Loading**

    ---

    Automatic cascading of `.env` → `.env.local` → `.env.{env}` → `.env.{env}.local`
    so you can layer base, local, and environment-specific config.

-   :material-check-decagram:{ .lg .middle } **Validation**

    ---

    Numeric constraints (`ge`, `le`, `gt`, `lt`), string constraints (`min_length`,
    `max_length`, `regex`), choice validation, and collection size constraints.

-   :material-file-document-multiple:{ .lg .middle } **Configuration Documentation**

    ---

    Generate docs in table, markdown, JSON, HTML, and dotenv formats with `describe()`.
    Auto-generate `.env.example` files with `generate_env_example()`.

-   :material-label:{ .lg .middle } **Environment Prefixes**

    ---

    Class-level `env_prefix` namespaces environment variables so multiple config classes
    coexist without collisions.

-   :material-refresh:{ .lg .middle } **Configuration Reload**

    ---

    Reload configuration at runtime without creating new instances — perfect for
    responding to `SIGHUP` or hot config updates.

</div>

---

## Installation

```bash
pip install dotenvmodel
```

Or with [uv](https://docs.astral.sh/uv/):

```bash
uv add dotenvmodel
```

!!! tip "Python 3.12+"
    dotenvmodel requires Python 3.12 or newer. It has a single runtime dependency:
    `python-dotenv`.

---

## Quick Start

```python
from dotenvmodel import DotEnvConfig, Field

class AppConfig(DotEnvConfig):
    # Required fields (Pydantic-style)
    database_url: str = Field(...)
    api_key: str = Field(...)

    # Optional with defaults and validation
    debug: bool = Field(default=False)
    port: int = Field(default=8000, ge=1, le=65535)
    workers: int = Field(default=4, ge=1, le=16)

    # Collection types
    allowed_hosts: list[str] = Field(default_factory=list)

# Load configuration from cascading .env files
config = AppConfig.load(env="dev")

# Access configuration with full type safety and IntelliSense
print(f"Connecting to {config.database_url}")  # config.database_url: str
print(f"Running on port {config.port}")        # config.port: int
print(f"Debug mode: {config.debug}")           # config.debug: bool

# Generate documentation for your configuration
print(AppConfig.describe())
```

---

## Next Steps

- [:material-download: Installation](getting-started/installation.md) — Set up dotenvmodel in your project
- [:material-rocket-launch: Quick Start](getting-started/quick-start.md) — Go from zero to loaded config in minutes
- [:material-shape: Supported Types](guides/types.md) — UUID, Decimal, datetime, SecretStr, URLs, JSON, and more
- [:material-check-decagram: Validation](guides/validation.md) — Numeric, string, choice, and collection constraints
- [:material-file-cog: Loading Configuration](guides/loading.md) — `.env` cascading, overrides, and dictionaries
- [:material-label: Environment Prefixes](guides/prefixes.md) — Namespace variables across multiple config classes
- [:material-file-document-multiple: Configuration Documentation](guides/configuration-docs.md) — `describe()` and `generate_env_example()`
- [:material-api: API Reference](api-reference/dotenvmodel.md) — Full autogenerated API docs
